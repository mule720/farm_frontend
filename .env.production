# Production environment variables for the React frontend build.
# Replace the placeholder URL with your actual production backend URL.
# This file is safe to commit — it contains no secrets, only a URL.

VITE_API_URL=https://api.yourdomain.com/graphql/
